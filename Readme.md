```
flatpak remote-add --user --if-not-exists fluxer-canary https://yofukashino.github.io/app.fluxer.FluxerCanary/index.flatpakrepo
flatpak install --user app.fluxer.FluxerCanary
```

pros - build from source 

cons - i wrote the manifest
